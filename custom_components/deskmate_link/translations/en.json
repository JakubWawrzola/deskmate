{
  "config": {
    "step": {
      "user": {
        "title": "Pair a Deskmate device",
        "description": "Home Assistant will generate a pairing code. Paste it into Deskmate (first-run screen or Settings -> Deskmate Link) on the computer you want to pair. The code carries the key and this Home Assistant's address, so there is nothing else to type. The entry binds itself to the first computer that authenticates with it.\n\nIf an entry is already waiting for pairing, its code is shown again instead of creating another one."
      },
      "show_key": {
        "title": "Pairing code",
        "description": "Copy this pairing code into Deskmate:\n\n`{code}`\n\nOlder Deskmate versions take only the bare key:\n\n`{key}`\n\nUpdate this integration before Deskmate on the computer. Deskmate 0.6 speaks protocol v2 only."
      },
      "new_key": {
        "title": "New pairing code",
        "description": "The old key stops working as soon as you confirm. Copy the new pairing code into Deskmate:\n\n`{code}`\n\nBare key for older Deskmate versions:\n\n`{key}`"
      },
      "unbind": {
        "title": "Unbind this entry",
        "description": "Currently bound to `{node_id}`. After unbinding, the entry attaches to the next computer that authenticates with its pairing key. Entities and their history are kept."
      },
      "cascade_on": {
        "title": "Enable cascade encryption",
        "description": "Adds a second encryption layer: ChaCha20-Poly1305 wrapped around AES-256-GCM, with its own key. It only helps if one of the two ciphers is ever broken. Both keys are stored in the same places (here and in Deskmate), so it does not help if a key is stolen. Protocol v2 already derives fresh session keys from an ephemeral X25519 exchange.\n\nBoth sides must have it on. Paste this key into Deskmate under Geeky stuff, then confirm here. The connection drops until both ends match. It is shown only once:\n\n`{key}`"
      },
      "cascade_off": {
        "title": "Disable cascade encryption",
        "description": "Traffic goes back to a single AES-256-GCM layer. Turn it off in Deskmate as well, otherwise that computer will not reconnect."
      }
    },
    "menu_options": {
      "new_key": "Generate a new pairing key",
      "unbind": "Unbind from the current computer",
      "cascade_on": "Enable cascade encryption (second layer)",
      "cascade_off": "Disable cascade encryption"
    },
    "abort": {
      "already_configured": "This node is already paired.",
      "reconfigure_successful": "Done.",
      "pending_reused": "An entry is already waiting for pairing. Its code was shown again - nothing new was created."
    }
  },
  "issues": {
    "unknown_node": {
      "title": "Deskmate: an unpaired computer keeps connecting",
      "description": "A computer calling itself `{node}` ({ip}) keeps failing the Deskmate Link handshake. No paired entry matches that name, or its pairing key is different.\n\nEither pair it (Add integration -> Deskmate Link, then paste the key into Deskmate), or - if it should use an entry that already exists - open that entry, choose Reconfigure -> Unbind from the current computer, and let it re-attach."
    }
  }
}
